# Database configuration
DATABASE_URL=postgresql://username@localhost:5432/database_name

# Application settings - IMPORTANT: No trailing slashes in URLs
APP_URL=https://insyte-backend-cc.onrender.com
FRONTEND_URL=https://insyte-io-dashboard-cc.onrender.com

# IMPORTANT: OAuth callback will be at ${APP_URL}/auth/youtube/callback
# Make sure this matches exactly with what's configured in Google Cloud Console

# YouTube API credentials
YOUTUBE_CLIENT_ID=your_youtube_client_id
YOUTUBE_CLIENT_SECRET=your_youtube_client_secret

# Stripe API credentials 